<?php

/**
 * Description of apelidoActions
 *
 * @author Uirapuru
 */
abstract class tcActions extends sfActions
{
    
    protected function tr($text, $args = null, $file = "messages")
    {
        return sfContext::getInstance()->getI18n()->__($text, $args, $file);
    }
}
